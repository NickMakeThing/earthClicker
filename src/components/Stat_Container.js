import React, {useState} from "react";

const Stat_Container = React.memo(() => {
    return (
        <div style={{display:'none'}}>
            
        </div>
    )
})

export default Stat_Container;
